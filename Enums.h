#pragma once

enum LEVEL
{
    GAME, STATUS, ERROR
};

enum OUTPUT
{
    CMD, FILEOUT
};

enum ASSIGMENTS
{
    LEFT,
    RIGHT,
    UP,
    DOWN,
    NOTHING,
    SAVE,
    LOAD,
    EXIT
};

enum DIFFICULTY {
    EASY = 1,
    HARD = 2
};