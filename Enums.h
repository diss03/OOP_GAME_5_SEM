#pragma once

enum LEVEL
{
    GAME, STATUS, ERROR
};

enum OUTPUT
{
    CMD, FILEOUT
};

enum ASSIGMENTS
{
    LEFT,
    RIGHT,
    UP,
    DOWN,
    NOTHING,
    EXIT,
    SAVE,
    LOAD
};

enum DIFFICULTY {
    EASY = 1,
    HARD = 2
};